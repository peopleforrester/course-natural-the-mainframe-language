[package]
name = "natural-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

# natural-core deliberately has NO wasm dependencies, so the TDD loop runs at native
# speed and the interpreter stays portable. See research/06-rust-wasm-toolchain.md.
[dependencies]
