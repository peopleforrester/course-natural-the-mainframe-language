# Pinned per docs/gotchas-rust-wasm.md so the build is reproducible and the wasm
# target installs automatically. Verified current stable as of 2026-09-17.
[toolchain]
channel = "1.98.1"
targets = ["wasm32-unknown-unknown"]
